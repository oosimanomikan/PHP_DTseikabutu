<div id="category">
    <ul>
        {# http://localhost/DT/shopping/ #}
        <li>
            <a href="{{constant('root\\Bootstrap::ENTRY_URL')}}root\User\Start_page\list.php">全て</a>
        </li>
        {% for value in cateArr %}
            {# 検証モードでURLをメモ帳で確認して、遷移先とGETの情報を表示 #}
            <li>
                <a href="{{constant('root\\Bootstrap::ENTRY_URL')}}root\User\Start_page\list.php?ctg_id={{value.ctg_id}}">
                    {{value.category_name}}
                </a>
            </li>
        {% endfor %}
    </ul>
</div>